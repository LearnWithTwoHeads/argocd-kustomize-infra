application:
  name: cert-manager
  namespace: cert-manager
  repoURL: https://charts.jetstack.io
  chart: cert-manager
  targetRevision: 1.18.2
  values: |
    extraArgs:
      - --enable-gateway-api
    crds:
      enabled: true
